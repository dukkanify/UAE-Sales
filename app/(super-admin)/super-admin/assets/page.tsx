import { AssetManagerShell } from "@/features/assets/components/asset-manager-shell";

export const metadata = { title: "Asset manager" };

export default function AssetManagerPage() {
  return <AssetManagerShell />;
}
